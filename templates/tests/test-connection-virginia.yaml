apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "static-web.fullname" . }}-virginia-test-connection"
  labels:
{{ include "static-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "kapil.virginia.fullname" . }}:{{ .Values.virginiaApi.service.port }}']
  restartPolicy: Never

