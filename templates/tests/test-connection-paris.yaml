apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "static-web.fullname" . }}-paris-test-connection"
  labels:
{{ include "static-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "kapil.paris.fullname" . }}:{{ .Values.parisApi.service.port }}']
  restartPolicy: Never

