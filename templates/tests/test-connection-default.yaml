apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "static-web.fullname" . }}-default-test-connection"
  labels:
{{ include "static-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "kapil.default.fullname" . }}:{{ .Values.defaultApi.service.port }}']
  restartPolicy: Never

